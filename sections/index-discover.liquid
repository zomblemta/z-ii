<style>
  .discover-image-wrapper {
    position: relative;
    width: 100%;
    overflow: hidden;
  }
  .discover-image-wrapper img {
    width: 100%;
    height: 100%;

    /* 初始位置：向下偏移并透明 */
    transform: translateY(30px);
    opacity: 0;

    /* 应用动画 */
    animation: fadeUpIn 1s ease-out forwards;

    /* 延迟动画（可选） */
    animation-delay: 0.3s;
  }

  @keyframes fadeUpIn {
    0% {
      opacity: 0;
      transform: translateY(30px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<div class="w-full px-[15px] md:px-[53px] mt-[30px]  flex flex-col gap-[30px] md:gap-[42px]">
  <h3 class="m-0 text-xl md:text-5xl font-semibold leading-normal uppercase tracking-[2px]">
    {{ section.settings.title }}
  </h3>
  <div class="grid grid-cols-1 md:grid-cols-3 gap-[30px] md:gap-[36px] ">
    {% for block in section.blocks %}
      <a href="{{ block.settings.url }}">
        <div class="flex flex-col  transition-transform duration-350ms ease-out origin-center {% if section.settings.image_style == 'style-2' %} relative {% endif %}">
          <div class="discover-image-wrapper">
            {%
              render 'alp-responsive-image',
              image: block.settings.image,
              image_mobile: block.settings.image,
              alt: block.settings.title,
              class:
              section.settings.image_style == 'style-1'
              | append: ' aspect-[430/573] md:aspect-1/1'
              | default: ' aspect-1/1 md:aspect-1/1'
            %}
          </div>
          <div class="flex flex-col {% if section.settings.image_style == 'style-2' %} absolute top-0 left-0 w-full h-full flex items-center justify-center {% endif %}">
            <span class="text-lg font-normal md:text-2xl font-Montaga leading-[18px] uppercase mt-[12px] md:mt-[30px]">
              {{ block.settings.title }}
            </span>
            <span class="text-xs font-light leading-[16px] mt-[6px] md:mt-[20px]">
              {{ block.settings.description }}
            </span>
          </div>
        </div>
      </a>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Index Discover",
  "blocks": [
    {
      "type": "discover",
      "name": "Discover",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description"
        },
        {
          "type": "url",
          "id": "url",
          "label": "URL"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "select",
      "id": "image_style",
      "label": "Image Position",
      "options": [
        {
          "value": "style-1",
          "label": "Style 1"
        },
        {
          "value": "style-2",
          "label": "Style 2"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Index Discover"
    }
  ]
}
{% endschema %}
